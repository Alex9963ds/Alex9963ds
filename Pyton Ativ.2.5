def inverter_palavras(frase):
    """Inverte as letras de cada palavra em uma frase."""

    palavras = frase.split()
    
    palavras_invertidas = [palavra[::-1] for palavra in palavras]
    
    nova_frase = ' '.join(palavras_invertidas)
    
    return nova_frase

frase_original = input("A cidade está cheia ")
frase_invertida = inverter_palavras(frase_original)
print(f"Frase com palavras invertidas: {frase_invertida}")
